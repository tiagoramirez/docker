version: "3.8"

services:
  app:
    build: .
    environment:
      - MONGO_URL=mongodb://db:27017/test
    depends_on:
      - db
    ports:
      - "6777:3000"
    volumes:
      - .:/usr/src/
      - /usr/src/node_modules
    command: npx nodemon -L index.js
  db:
    image: mongo